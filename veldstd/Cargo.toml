[workspace]
members = [
    "ui-service/module/generated",
    "ui-service/lib/rust",
]
resolver = "2"

[workspace.lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("pdk"))'] }

[profile.release]
opt-level = "s"
panic = "abort"
lto = true
codegen-units = 1