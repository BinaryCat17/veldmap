[package]
name = "veldmap-rust-rpc"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13"
serde = { version = "1.0", features = ["derive"], optional = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
extism-pdk = "1.4"
anyhow = "1.0"

[build-dependencies]
prost-build = { version = "0.13" }
