[package]
name = "{{ module_name }}"
version = "{{ version }}"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
veldsdk = { path = "{{ sdk_path }}" }
{% for name, version in dependencies.items() %}
{{ name }} = {{ version }}
{% endfor %}
