# Мутации отбора растров продукта и файла координат.
# Формат — buildgen/mutate.py; крейт — модуль data-provider.

имя: густота сетки не участвует в отборе
файл: veldmodules/data-provider/src/imagery.rs
было:                     grid_rank(key),
стало:                     0,

имя: полукилометровой сетке достаётся опорная решётка
файл: veldmodules/data-provider/src/imagery.rs
было:         let order = match grid_rank(raster).0 == 0 {
стало:         let order = match false {

имя: названный файл ложится подробным, даже если он не растр продукта
файл: veldmodules/data-provider/src/imagery.rs
было:     } else if !is_raster(wanted) {
стало:     } else if false {

имя: чужой адрес подписывается своими ключами
файл: veldmodules/data-provider/src/s3.rs
было:     if parsed.scheme() != "https" || parsed.host_str() != Some(HOST) {
стало:     if false {
