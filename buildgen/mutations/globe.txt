# Мутации глобуса: запасные растры наложения.
# Формат — buildgen/mutate.py; крейт — модуль globe.

имя: запасной не переносит свой номер
файл: veldmodules/globe/src/overlay.rs
было:         fresh.ordinal = ordinal;
стало:         fresh.ordinal = self.ordinal;

имя: запасной встаёт с прежним знанием о файле
файл: veldmodules/globe/src/overlay.rs
было:         *self = fresh;
стало:         self.resource = fresh.resource; self.geolocation = fresh.geolocation; self.spares = fresh.spares; self.ordinal = fresh.ordinal;

имя: предел детали ложится к подробному, даже когда деталь решает превью
файл: veldmodules/globe/src/overlay.rs
было:         let cap_about_detailed = self.decider().is_some_and(|raster| raster.role == Role::Detailed);
стало:         let cap_about_detailed = self.decider().is_some();

имя: отказ превью попадает в слова о подробном
файл: veldmodules/globe/src/overlay.rs
было:                     Role::Preview => rest.push(trouble.clone()),
стало:                     Role::Preview => about_detailed.push(trouble.clone()),

имя: величина в отчёте — у превью, когда подробный есть
файл: veldmodules/globe/src/overlay.rs
было:                 .and_then(|raster| raster.meta.as_ref())
стало:                 .and_then(|_| self.raster(Role::Preview)).and_then(|raster| raster.meta.as_ref())

имя: другая величина у лежащего растра не замечается
файл: veldmodules/globe/src/overlay.rs
было:                 asked != raster.variable
стало:                 false

имя: отказ названной величине уводит к запасному файлу
файл: veldmodules/globe/src/overlay.rs
было:         if !self.variable.is_empty() {
стало:         if false {

имя: тот же набор с другой величиной принимается как прежний
файл: veldmodules/globe/src/module.rs
было:             if state.overlays[index].variables != incoming_variables {
стало:             if false {

имя: отказ названной величине хоронит слой
файл: veldmodules/globe/src/overlay.rs
было:             && self.rasters.iter().all(|raster| raster.variable.is_empty())
стало:             && true
