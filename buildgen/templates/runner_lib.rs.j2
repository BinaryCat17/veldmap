// AUTO-GENERATED BY buildgen/generate.py - DO NOT EDIT
// Композиционный корень раннера `{{ runner }}` (runners/{{ runner }}/runner.yaml):
// список нативных модулей, которые этот раннер собирает в себе. Раннер зовёт
// register_all() один раз после init_core_services и до загрузки плагинов.
//
// Набор модулей — свойство раннера, а не платформы: десктоп и мобильная
// сборка берут разные списки из своих runner.yaml, а не расходятся по
// #[cfg(target_os)] в коде.

/// Регистрирует все модули раннера в шине.
pub fn register_all(ctx: std::sync::Arc<veldmap_host_util::HostContext>) {
{%- for m in modules %}
    // {{ m.name }}
    {{ m.crate_snake }}::register_services(ctx.clone());
{%- endfor %}
}
