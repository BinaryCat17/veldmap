//! Публичный API tile-cache для модулей-потребителей: типы контракта и то, из
//! чего сложен сам тайл.

pub use super::proto::*;

// Единственный экземпляр правил тайла, общий с самим модулем: файл включается
// и сюда, и в crate::module (см. заголовок layout.rs). Формат тайла и потолки
// его размера — это то, о чём кэш и производитель обязаны говорить одно и то
// же: их тайлы ложатся в один кадр.
#[path = "../../../src/layout.rs"]
pub mod layout;
