package: veldmap-tile-cache
language: rust
rust:
  dependencies:
    prost: "0.14"
    serde:
      version: "1.0"
      features: ["derive"]
    serde_json: "1.0"
    anyhow: "1.0"
    log: "0.4"
    # Кодек тайлов на диске — тот же, которым пишет производитель.
    qoi: "0.4"
