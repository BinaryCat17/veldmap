name: ui-service
version: 0.1.0

interface:
  inputs:
    set_view:
      type: ui/SetViewRequest
    handle_ui_event:
      type: app/UiEvent

dependencies:
  app:
    calls:
      display:
        type: app/AppDisplayCommand
