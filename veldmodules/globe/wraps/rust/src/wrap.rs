//! Публичный API globe для модулей-потребителей: типы контракта и та же
//! геодезия, которой он рисует.

pub use super::proto::*;

// Единственный экземпляр перехода от географических координат к декартовым,
// общий с самим модулем: файл включается и сюда, и в crate::module.
//
// Общий он не для экономии строк. Вопрос «попал ли щелчок в снимок» и вопрос
// «где нарисовать его контур» обязаны отвечаться одной арифметикой: контур
// рисуется дугами больших кругов, и «внутри» на экране значит ровно то же
// самое только тогда, когда считающий и рисующий пользуются одной формулой.
// Разойдясь на глаз, две такие формулы расходятся на десятки километров — и
// увидеть это можно будет только по снимку, который не выбирается щелчком.
#[path = "../../../src/geodesy.rs"]
pub mod geodesy;
