# Показ на шаре — не выбор: коробочка в списке остаётся пустой. И это
# переключатель: тот же значок снимает то, что положил.
#
# Показывают, чтобы посмотреть, и показанное живёт своим списком («На
# просмотре»); выбирают, чтобы что-то с набранным сделать. Ставя заодно
# коробочку, показ клал бы снимок в состав следующего пакетного действия.
#
# Список сужается фильтром по той же причине, что и в outline.txt: значок шара
# есть только у снимка, а первой строкой «Скачанного» бывает файл.
#
# Нужно: хотя бы один скачанный снимок .SAFE в runtime/data и сеть — продукт
# под ключом строки восстанавливает каталог.
100 timeout 25000
500 wait tab_menu
700 tap tab_menu
1100 tap new_tab:downloaded
1900 wait query
2100 tap query
2400 type SAFE
3400 wait globe_toggle#1
3600 absent text:выбрано
3800 tap globe_toggle#1
# Показ увёл на вкладку шара и назвал снимок полосой под ним.
4600 wait text:Снять с шара
4800 tap text:Скачанное
5600 wait check_shown
5800 absent text:выбрано
# Второе нажатие по тому же значку снимает снимок с шара — и вкладка слоёв
# пустеет вместе с ним.
6000 tap globe_toggle#1
6800 tap tab_menu
7200 tap new_tab:shown
8000 gone overlay_remove
8200 exit
