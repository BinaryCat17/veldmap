# Гранула SLSTR уровня 1 (SL_1_RBT) с диска: подробный растр выбирается по
# густоте сетки, и у ночной гранулы он пуст — видимые каналы записаны сплошным
# «нет данных». Слой от этого не снимается: провайдер назвал за выбранным
# запасной (`imagery::spares`), глобус ставит его на то же место
# (`Raster::next_spare`), а строка слоя называет файл, который лёг подробным, —
# перед словами о нём: «F1_BT_fn.nc · не подробнее превью: подробный растр на
# шар не кладётся» целиком в подсказке, в строке — сколько влезло. Километровый
# тепловой канал в 1500 столбцов у́же квиклука в 2422.
#
# Нужна гранула на диске: runtime/data/dem/source/S3A_SL_1_RBT____20260824T174507_….SEN3
# (198 файлов, скачана приложением); по сети её не качают — это минуты.
#
# Запрос сужает «Скачанное» до одной строки, поэтому значок шара один и без
# номера. Ждётся имя запасного файла в строке слоя: оно появляется только после
# того, как выбранный отвергнут, запасной описан и глобус назвал его номером, —
# то есть само собой доказывает и запасной, и имя.
100 timeout 120000
500 wait tab_menu#1
700 tap tab_menu#1
1100 tap new_tab:downloaded
1900 wait query
2100 tap query
2400 type S3A_SL_1_RBT
4400 wait globe_toggle
4600 tap globe_toggle
5200 tap tab_menu#1
5600 tap new_tab:shown
6400 wait overlay_remove
7000 wait text:F1_BT_fn.nc
12000 expect overlay_remove
12200 shot uitest-slstr
12400 exit
